# 우리 동네 카페 지도 — PRD

> 상태: 초안 (v0.3) · 작성일: 2026-07-27 · 코드 미착수 (문서 단계)
> 변경: `CLAUDE.md` 구현 규칙에 맞춰 스택(Vite)·환경 변수(`VITE_`)·좌표 변환(순차)·성능 지표 동기화
> 결정 이력은 §14 참조

---

## 1. 개요

엑셀로 정리해 둔 카페 목록을 업로드하면, 주소를 좌표로 변환해 카카오맵 위에 마커로 표시한다.
사용자는 마커를 눌러 방문 여부를 체크하고 한 줄 소감을 남길 수 있으며, 로그인 후에는 그 기록이
계정에 저장되어 다시 방문했을 때 그대로 보인다. 방문 체크한 카페만 따로 모아보는 목록 화면도 제공한다.

### 1.1 문제 정의

- 동네 카페 목록을 엑셀/노션 등으로 관리하는 사람은 많지만, **위치를 한눈에 보기 어렵다.**
- 어디를 갔고 안 갔는지, 갔다면 어땠는지 기억이 흩어진다.
- 지도 서비스의 "저장" 기능은 소감을 남기기 불편하고, 내 목록을 파일로 관리할 수 없다.

### 1.2 해결 방향

엑셀(내가 관리하는 원본) + 지도(위치 파악) + 개인 기록(방문/소감)을 한 화면에서 잇는다.
엑셀은 **매번 업로드하는 임시 입력**으로 취급하고, 방문 기록만 서버에 영구 저장한다.

### 1.3 타깃 사용자

| 구분 | 설명 |
|---|---|
| 주 사용자 | 카페 목록을 스프레드시트로 관리하며, 동네 카페를 순회하는 개인 |
| 사용 규모 | 1회 업로드 시 카페 10~500개 수준 |
| 사용 환경 | 모바일 웹 우선, 데스크톱 웹 병행 |

---

## 2. 목표 / 비목표

### 2.1 목표 (MVP)

1. 엑셀 업로드 → 카페 목록 파싱
2. 주소 → 좌표 변환 후 카카오맵 마커 표시 (실패 주소는 화면에 안내)
3. 마커 클릭 → 방문 체크 + 한 줄 소감 입력
4. 회원가입/로그인 → 본인 소감 저장, 같은 카페 재클릭 시 이전 소감 표시
5. 방문 체크한 카페만 모아보는 목록 화면 (엑셀 없이도 조회, 본인 것만)
6. 카테고리로 지도 마커 필터링

### 2.2 비목표 (MVP 범위 밖)

- 다른 사람의 소감 보기 / 공유 / 소셜 기능
- 사진 업로드, 별점, 태그, 여러 줄 리뷰
- 카페 정보 자동 수집(크롤링), 영업시간·메뉴·전화번호
- 경로 안내, 길찾기, 주변 검색
- 팀/그룹 공동 편집
- 오프라인 지원, 네이티브 앱
- 소셜 로그인(카카오 로그인) — 향후 확장
- 방문 기록 엑셀 내보내기 — 향후 확장
- 사용자가 방문 날짜를 직접 입력하는 기능 — 저장 시각만 자동 기록

> ⚠️ **미정 항목:** 엑셀 목록 자체의 서버 저장 여부는 아직 결정되지 않았다.
> 본 문서는 **"저장하지 않음(세션 한정)"** 을 전제로 작성되어 있다. §12-A 참조.

---

## 3. 핵심 개념

### 3.1 카페 식별 = 이름 + 주소

이 서비스에는 카페 마스터 테이블이 없다. 엑셀이 곧 원본이며, 서버는 **사용자가 기록을 남긴 장소만**
안다. 따라서 장소의 동일성은 `이름 + 주소` 조합으로 판단한다.

- 같은 사용자가 같은 `(이름, 주소)` 에 남긴 기록은 **항상 1개만 유지된다.** (§7.2 참조)
- 다시 저장하면 새 행이 생기지 않고 기존 행이 갱신된다(upsert).
- 이름이나 주소 문자열이 달라지면 **다른 장소로 취급된다.** (엑셀을 수정하면 기록이 연결되지 않을 수 있음 → §11 위험)

### 3.2 정규화 규칙

동일성 판단의 안정성을 위해 저장·비교 시 아래 정규화를 적용한다.

- 앞뒤 공백 제거, 연속 공백 1칸으로 축약
- 유니코드 NFC 정규화
- 대소문자는 구분하지 않음 (영문 카페명 대응)
- 원본 문자열도 함께 보관해 화면에는 사용자가 입력한 그대로 표시

---

## 4. 사용자 플로우

### 4.1 첫 방문 (비로그인)

```
접속 → 엑셀 업로드 → 좌표 변환 진행률 표시 → 지도에 마커 표시
     → 실패 주소 목록 안내
     → 마커 클릭 → "로그인이 필요합니다" 안내 → 로그인/회원가입
```

### 4.2 로그인 사용자

```
로그인 → 엑셀 업로드 → 지도 + 마커
       → 마커 클릭 → 바텀시트/모달
           ├─ 기존 기록 있음 → 방문 체크 상태 + 소감이 채워진 상태로 표시 → 수정/삭제
           └─ 기존 기록 없음 → 빈 상태 → 방문 체크 + 소감 입력 → 저장
       → 마커 색상 즉시 갱신 (미방문 ↔ 방문)
```

### 4.3 방문 목록 화면 (엑셀 불필요)

```
로그인 → "내 방문 목록" → 본인이 방문 체크한 카페 목록 (이름/주소/소감/기록 시각)
       → 항목 클릭 → 지도에서 해당 위치로 이동 (좌표가 저장된 경우)
       → 소감 수정 / 기록 삭제
```

---

## 5. 기능 요구사항

### 5.1 F-1. 엑셀 업로드 및 파싱

**입력 형식**

| 열 | 필수 | 설명 |
|---|---|---|
| 이름 | ✅ | 카페명 |
| 주소 | ✅ | 도로명 또는 지번 주소 |
| 카테고리 | ⬜ | 자유 문자열 (예: 디저트, 로스터리, 작업용) |

- 헤더 행은 첫 행에 존재해야 한다.
- 헤더 인식은 별칭을 허용한다: `이름 | 카페명 | name`, `주소 | address | 소재지`, `카테고리 | category | 분류`
- 지원 확장자: `.xlsx`, `.xls`, `.csv`
- 첫 번째 시트만 읽는다.

**요구사항**

| ID | 내용 |
|---|---|
| F-1.1 | 파일 선택 및 드래그&드롭으로 업로드할 수 있다. |
| F-1.2 | 파싱은 브라우저에서 수행한다(서버 업로드 없음). |
| F-1.3 | 필수 열이 없으면 업로드를 중단하고 어떤 열이 없는지 안내한다. |
| F-1.4 | 이름 또는 주소가 빈 행은 건너뛰고, 건너뛴 행 수와 행 번호를 안내한다. |
| F-1.5 | 파일 내 `(이름, 주소)` 중복 행은 1건으로 합치고 합쳐진 건수를 안내한다. |
| F-1.6 | 파싱 결과 요약을 표시한다: 총 N건 / 유효 N건 / 제외 N건. |
| F-1.7 | 최대 500행까지 처리한다. 초과분은 처리하지 않고 초과 사실을 안내한다. |
| F-1.8 | 업로드한 목록은 서버에 저장하지 않으며 새로고침 시 사라진다. |

**제한**

- 파일 크기 상한: 5MB
- 행 수 상한: 500행

### 5.2 F-2. 주소 → 좌표 변환 및 지도 표시

| ID | 내용 |
|---|---|
| F-2.1 | 카카오 로컬 주소 검색을 사용해 주소를 위경도로 변환한다. |
| F-2.2 | 변환은 프론트엔드에서 직접 호출한다. (카카오맵 SDK `libraries=services`) |
| F-2.3 | 변환 중 진행률을 표시한다 (예: `123 / 250 변환 중`). |
| F-2.4 | 변환 성공한 카페를 카카오맵 마커로 표시한다. |
| F-2.5 | 마커 전체가 보이도록 초기 지도 범위를 자동 조정한다. |
| F-2.6 | 마커가 밀집한 구간은 클러스터링으로 묶어 표시한다. |
| F-2.7 | 변환 실패 주소는 **화면에 목록으로 안내한다** (이름, 주소, 원본 행 번호, 실패 사유). |
| F-2.8 | 실패 목록에서 주소를 직접 수정해 재시도할 수 있다. |
| F-2.9 | 동일 주소는 요청을 1회로 합친다(중복 제거). |
| F-2.10 | 변환 결과는 세션 동안 캐시해 같은 주소를 재요청하지 않는다. |
| F-2.11 | 마커는 방문 여부에 따라 시각적으로 구분한다 (미방문 / 방문). |
| F-2.12 | 엑셀의 카테고리 값으로 마커를 필터링할 수 있다. (§5.6) |
| F-2.13 | 변환은 **한 건씩 순차로** 처리한다. 병렬 호출하지 않는다. |
| F-2.14 | 변환된 마커는 완료를 기다리지 않고 **도착하는 대로 즉시** 지도에 표시한다. |
| F-2.15 | 진행 중 변환을 중단할 수 있다. 중단 시점까지 변환된 마커는 유지된다. |
| F-2.16 | 마커를 다시 그릴 때는 기존 마커를 모두 제거한 뒤 새로 그린다. |
| F-2.17 | 방문 체크한 카페의 마커는 미방문과 **다른 색**으로 표시한다. (색만으로 구분하지 않도록 모양도 함께 다르게 한다 — §9 접근성) |

**변환 실패 사유 분류**

| 사유 | 화면 안내 문구(예) |
|---|---|
| 검색 결과 없음 | "주소를 찾지 못했습니다. 도로명 주소로 다시 입력해 보세요." |
| 주소 형식 오류 | "주소 형식을 인식할 수 없습니다." |
| 호출 실패/네트워크 | "일시적인 오류입니다. 다시 시도해 주세요." (재시도 버튼 제공) |
| 호출 한도 초과 | "요청이 많아 잠시 후 다시 시도해 주세요." |

**호출 정책**

- **순차 처리:** 한 번에 1건씩, 앞 요청이 끝난 뒤 다음 요청을 보낸다. 병렬 호출하지 않는다.
- 루프 진입 전 동일 주소를 중복 제거하고, 캐시에 있으면 호출하지 않는다.
- 실패 시 지수 백오프로 최대 2회 재시도
- 주소 검색으로 실패하면 키워드 검색으로 1회 보조 시도
- 순차 처리 자체가 호출 한도 방어 수단이므로 별도 동시성 제한은 두지 않는다.

> **순차 처리의 대가:** 전체 완료 시간이 길어진다(§9). 대신 첫 마커가 즉시 보이고(F-2.14),
> 중단할 수 있어서(F-2.15) 체감 대기는 완료 시간과 다르다. 진행률(F-2.3)로 상태를 계속 알린다.

### 5.3 F-3. 방문 체크 + 한 줄 소감

| ID | 내용 |
|---|---|
| F-3.1 | 마커 클릭 시 카페 이름·주소·카테고리와 함께 입력 UI를 표시한다. |
| F-3.2 | 방문 여부를 체크박스/토글로 설정한다. |
| F-3.3 | 한 줄 소감을 텍스트로 입력한다. (최대 200자, 남은 글자 수 표시) |
| F-3.4 | 저장 시 즉시 마커 상태와 목록에 반영한다. |
| F-3.5 | 기록을 삭제할 수 있다. (확인 절차 후) |
| F-3.6 | 비로그인 상태에서는 입력 UI를 잠그고 로그인을 안내한다. |
| F-3.7 | 저장 실패 시 오류를 표시하고 입력 내용을 잃지 않는다. |

**저장 규칙**

- 방문 체크만 하고 소감을 비워도 저장된다.
- **소감만 쓰고 방문 체크를 하지 않아도 저장된다.** (예: "주말에만 영업", "가보고 싶다")
  - 단, §5.5 방문 목록 화면에는 나오지 않는다. (`visited = false` 이므로)
  - 기록이 사라진 것으로 오해하지 않도록, 저장 직후 안내한다:
    *"저장했어요. 방문 체크를 하면 내 방문 목록에서도 볼 수 있어요."*
  - 지도에서 마커를 다시 클릭하면 이 소감은 그대로 보인다.
- 방문 체크를 해제해도 기록은 남으며 소감도 보존된다.
- 기록을 완전히 없애려면 "기록 삭제"를 사용한다. (F-3.5)

### 5.4 F-4. 회원가입 / 로그인 / 기록 복원

| ID | 내용 |
|---|---|
| F-4.1 | 이메일 + 비밀번호로 회원가입한다. |
| F-4.2 | 이메일 + 비밀번호로 로그인한다. |
| F-4.3 | 로그아웃한다. |
| F-4.4 | 새로고침·재접속 시에도 로그인 세션이 유지된다. |
| F-4.5 | 엑셀 업로드 후, 목록에 포함된 카페 중 본인 기록이 있는 것을 조회해 마커·입력 UI에 반영한다. |
| F-4.6 | 같은 카페를 다시 클릭하면 이전 소감이 그대로 보인다. |
| F-4.7 | 본인 기록만 조회·수정·삭제할 수 있다. (§7.3 RLS로 보장) |
| F-4.8 | 비밀번호 재설정을 지원한다. |

**F-4.5 조회 방식**

업로드된 카페의 `(정규화 이름, 정규화 주소)` 목록으로 본인 기록을 한 번에 조회한다.
카페 수가 많을 경우 100건 단위로 나눠 조회한다.

### 5.5 F-5. 방문 목록 화면

| ID | 내용 |
|---|---|
| F-5.1 | 엑셀 업로드 없이 접근 가능하다. |
| F-5.2 | 로그인한 본인의 기록만 조회한다. |
| F-5.3 | `방문 = true` 인 기록만 표시한다. |
| F-5.4 | 각 항목에 이름, 주소, 카테고리, 소감, 최근 수정 시각을 표시한다. |
| F-5.5 | 최근 수정 시각 내림차순으로 정렬한다. (이름순 정렬도 제공) |
| F-5.6 | 이름/주소/소감 텍스트로 검색한다. |
| F-5.7 | 목록에서 바로 소감을 수정하거나 기록을 삭제할 수 있다. |
| F-5.8 | 좌표가 저장된 항목은 클릭 시 지도에서 해당 위치로 이동한다. |
| F-5.9 | 기록이 없으면 빈 상태 화면과 안내 문구를 표시한다. |
| F-5.10 | 20건 단위 페이지네이션(또는 무한 스크롤)을 적용한다. |
| F-5.11 | 목록 상단에 정렬 전환 버튼(`최신순` / `이름순`)을 두고 현재 선택 상태를 표시한다. 기본값은 `최신순`(F-5.5)이며, 전환은 서버 재조회 없이 즉시 반영한다. 이름순은 한글 로케일 기준으로 비교한다. 선택은 새로고침 시 기본값으로 돌아간다. |

> **좌표 저장 이유:** 방문 목록 화면은 엑셀 없이 동작해야 한다. 기록 저장 시 좌표를 함께 넣어두면
> 목록에서 지도로 이동할 때 주소 변환을 다시 하지 않아도 된다. 좌표는 선택 항목이며, 없으면
> 지도 이동 버튼을 비활성화한다.

### 5.6 F-6. 카테고리 필터

| ID | 내용 |
|---|---|
| F-6.1 | 업로드한 엑셀의 카테고리 값에서 필터 목록을 자동으로 만든다. (하드코딩하지 않음) |
| F-6.2 | 카테고리는 다중 선택할 수 있으며, 선택된 카테고리의 마커만 지도에 표시한다. |
| F-6.3 | 아무것도 선택하지 않은 초기 상태는 "전체 표시"로 동작한다. |
| F-6.4 | 각 카테고리 옆에 해당 건수를 표시한다. (예: `로스터리 (12)`) |
| F-6.5 | 카테고리가 빈 카페는 `미분류` 그룹으로 묶어 함께 필터링한다. |
| F-6.6 | 방문 여부 필터(전체 / 방문 / 미방문)를 카테고리 필터와 함께(AND) 적용한다. |
| F-6.7 | 필터 적용 시 지도 범위를 자동으로 다시 맞추지 않는다. (보던 위치를 유지) |
| F-6.8 | 필터 결과가 0건이면 "조건에 맞는 카페가 없습니다" 안내를 표시한다. |
| F-6.9 | 필터는 클라이언트 메모리에서만 동작하며 서버 조회를 다시 하지 않는다. |
| F-6.10 | 필터 상태는 새로고침 시 초기화된다. (업로드 목록과 함께 사라짐) |

**카테고리 정규화**

- 필터 그룹화 시 앞뒤 공백 제거 + 대소문자 무시로 비교한다. (§3.2 동일 규칙)
- 화면 표시는 엑셀에 처음 등장한 표기를 그대로 쓴다.

> **참고:** 카테고리 필터는 지도 화면 전용이다. 방문 목록 화면(§5.5)의 필터링은
> 검색(F-5.6)으로 처리하며, 카테고리 필터 UI는 MVP에서 지도에만 둔다.

---

## 6. 화면 정의

| 화면 | 경로(예) | 인증 | 내용 |
|---|---|---|---|
| 홈 / 지도 | `/` | 불필요 | 업로드 영역, 지도, 마커, 카테고리·방문 필터, 실패 주소 패널 |
| 로그인 | `/login` | 불필요 | 이메일/비밀번호 |
| 회원가입 | `/signup` | 불필요 | 이메일/비밀번호 |
| 비밀번호 재설정 | `/reset-password` | 불필요 | 이메일 입력 → 메일 발송 |
| 내 방문 목록 | `/visits` | **필요** | 방문 체크한 카페 목록, 검색/정렬/수정/삭제 |

### 6.0 인증 경계

**지도는 공개, 기록은 로그인 필요.**

| 동작 | 비로그인 | 로그인 |
|---|---|---|
| 엑셀 업로드 | ✅ | ✅ |
| 지도·마커 보기 | ✅ | ✅ |
| 카테고리 필터 | ✅ | ✅ |
| 마커 클릭해 카페 정보 보기 | ✅ | ✅ |
| 방문 체크 / 소감 입력·수정·삭제 | ❌ 로그인 안내 | ✅ |
| 이전 소감 불러오기 | ❌ | ✅ |
| 내 방문 목록 (`/visits`) | ❌ 로그인 화면으로 이동 | ✅ |

- 비로그인 상태에서 마커를 클릭하면 정보는 보이고, 입력 영역만 잠긴 채 로그인 버튼이 표시된다.
- 로그인 후에는 **직전에 보고 있던 지도 상태로 돌아온다.** 업로드한 목록이 사라지지 않도록,
  로그인은 페이지 이동 없이 모달로 처리하거나 목록을 유지한 채 복귀시킨다.

### 6.1 홈 화면 구성

```
┌──────────────────────────────────────────────┐
│ 헤더  우리 동네 카페 지도    [내 방문 목록] [로그인] │
├──────────────────────────────────────────────┤
│ ┌──────────────┐                              │
│ │ 엑셀 업로드   │  총 250건 · 유효 248건 · 제외 2건 │
│ │ (드래그&드롭) │  좌표 변환 248/248 완료          │
│ └──────────────┘                              │
├──────────────────────────────────────────────┤
│ 카테고리  [로스터리 12] [디저트 8] [미분류 3]    │
│ 방문여부  ( 전체 ) ( 방문 ) ( 미방문 )           │
├──────────────────────────────────────────────┤
│                                              │
│                 카카오맵                       │
│         ● 미방문    ★ 방문                     │
│                                              │
├──────────────────────────────────────────────┤
│ ⚠ 주소를 찾지 못한 카페 2건            [펼치기]  │
│   · 3행  뭉카페 / 서울 어딘가  → 결과 없음 [수정] │
└──────────────────────────────────────────────┘
```

### 6.2 마커 클릭 시 (바텀시트 / 모달)

```
┌──────────────────────────────┐
│ 뭉카페                    [×] │
│ 서울 성동구 성수이로 10        │
│ 카테고리: 로스터리             │
├──────────────────────────────┤
│ [✓] 방문했어요                │
│ ┌──────────────────────────┐ │
│ │ 라떼가 고소했다            │ │
│ └──────────────────────────┘ │
│                     12 / 200 │
│ 2026-07-20 저장됨            │
├──────────────────────────────┤
│ [기록 삭제]          [저장]   │
└──────────────────────────────┘
```

---

## 7. 데이터 모델

### 7.1 저장 범위

| 데이터 | 저장 위치 | 영속성 |
|---|---|---|
| 사용자 계정 | Supabase Auth | 영구 |
| 방문 기록 / 소감 | Supabase Database (`cafe_notes`) | 영구 |
| 엑셀 업로드 목록 | 브라우저 메모리 | 세션 한정 |
| 좌표 변환 캐시 | 브라우저 메모리 (선택: sessionStorage) | 세션 한정 |

### 7.2 `cafe_notes` 테이블

| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| `id` | `uuid` | PK, default `gen_random_uuid()` | |
| `user_id` | `uuid` | NOT NULL, FK → `auth.users(id)` ON DELETE CASCADE | 소유자 |
| `cafe_name` | `text` | NOT NULL | 카페명 (원본 표기) |
| `cafe_address` | `text` | NOT NULL | 주소 (원본 표기) |
| `name_key` | `text` | NOT NULL | 정규화된 카페명 (§3.2) |
| `address_key` | `text` | NOT NULL | 정규화된 주소 (§3.2) |
| `category` | `text` | NULL 허용 | 업로드 당시 카테고리 |
| `visited` | `boolean` | NOT NULL, default `false` | 방문 여부 |
| `note` | `text` | NULL 허용, 길이 ≤ 200 | 한 줄 소감 |
| `lat` | `double precision` | NULL 허용 | 위도 (지도 이동용) |
| `lng` | `double precision` | NULL 허용 | 경도 |
| `created_at` | `timestamptz` | NOT NULL, default `now()` | |
| `updated_at` | `timestamptz` | NOT NULL, default `now()` | 트리거로 자동 갱신 |

**핵심 제약 — 1인 1장소 1기록**

```
UNIQUE (user_id, name_key, address_key)
```

- **같은 사람이 같은 장소(이름+주소)에 남긴 기록은 데이터베이스 수준에서 1개만 유지된다.**
- 저장은 항상 이 유니크 키를 대상으로 한 **upsert** 로 수행한다 → 재저장 시 새 행이 아니라 갱신.
  `insert` 를 사용하지 않는다. upsert 시 충돌 대상을 **명시**해야 한다
  (`onConflict: 'user_id,name_key,address_key'`). 명시하지 않으면 일반 insert 처럼 동작해
  유니크 제약 위반 오류가 발생한다.
- `user_id` 는 세션(`auth.uid()`)에서 가져온다. 클라이언트가 보낸 값을 신뢰하지 않는다.
- 애플리케이션 로직 실수와 무관하게 중복이 물리적으로 불가능하다.
- 다른 사용자는 같은 장소에 각자 자기 기록을 가진다. (`user_id` 가 키에 포함)

**인덱스**

| 인덱스 | 목적 |
|---|---|
| `UNIQUE (user_id, name_key, address_key)` | 중복 방지 + 마커 클릭 시 단건 조회 |
| `(user_id, visited, updated_at DESC)` | 방문 목록 화면 정렬 조회 |

**제약 조건**

- `CHECK (char_length(note) <= 200)`
- `CHECK (char_length(cafe_name) > 0 AND char_length(cafe_address) > 0)`

### 7.3 RLS (행 수준 보안)

`cafe_notes` 는 **RLS 를 활성화하며, 모든 접근은 본인 행으로 제한된다.**

| 정책 | 동작 | 조건 |
|---|---|---|
| `select_own` | SELECT | `auth.uid() = user_id` |
| `insert_own` | INSERT | `auth.uid() = user_id` (WITH CHECK) |
| `update_own` | UPDATE | `auth.uid() = user_id` (USING + WITH CHECK) |
| `delete_own` | DELETE | `auth.uid() = user_id` |

원칙:

- RLS 를 켜고 정책을 명시하기 전까지 테이블은 접근 불가 상태로 둔다.
- 프론트엔드는 **anon 키만** 사용한다. `service_role` 키는 클라이언트에 절대 포함하지 않는다.
- UPDATE 정책은 USING 과 WITH CHECK 를 모두 지정해 `user_id` 를 남의 것으로 바꾸는 시도를 막는다.
- 결과적으로 방문 목록 화면(§5.5)은 쿼리에 `user_id` 조건을 넣지 않아도 본인 것만 반환된다.
  (조건은 방어적으로 함께 넣는다.)
- 스키마와 정책은 **Supabase MCP 도구로 적용한다.** (§8.3)

> ⚠️ **RLS 의 함정:** 정책에 막힌 조회는 **오류가 아니라 빈 결과**를 반환한다.
> "데이터가 없음"과 "권한이 없음"이 화면상 구별되지 않으므로, 빈 목록을 보고 정상이라고
> 단정하면 안 된다. 정책 검증은 반드시 **서로 다른 계정 2개**로 교차 확인한다. (R-14)

---

## 8. 기술 구성

| 영역 | 선택 | 비고 |
|---|---|---|
| 빌드 도구 | **Vite** | 환경 변수 접두사 `VITE_` |
| UI | **React** | SPA (서버 렌더링 없음) |
| 스타일 | **Tailwind CSS + shadcn/ui** | |
| 언어 | TypeScript | |
| 지도 | 카카오맵 JavaScript SDK | 프론트엔드에서 직접 호출, `libraries=services` 필수 |
| 주소 변환 | 카카오 로컬 주소 검색 (`kakao.maps.services.Geocoder`) | 프론트엔드에서 직접 호출 |
| 엑셀 파싱 | **xlsx (SheetJS)** | 브라우저에서 파싱 |
| 인증 | Supabase Auth (이메일/비밀번호) | |
| 데이터베이스 | Supabase Database (PostgreSQL) + RLS | 스키마·정책은 Supabase MCP 도구로 적용 (§8.3) |
| 배포 | Vercel | 정적 빌드 |

### 8.1 카카오맵 호출 방식

- 지도 렌더링과 주소 변환 모두 **프론트엔드에서 직접** 호출한다. 별도 프록시 서버를 두지 않는다.
- SDK 는 **`libraries=services` 를 반드시 포함**해 불러온다. 이 파라미터가 없으면
  `kakao.maps.services.Geocoder` 가 존재하지 않아 주소→좌표 변환 자체가 불가능하다.
- `autoload=false` 로 불러온 뒤 `kakao.maps.load(callback)` 안에서 초기화한다.
  (스크립트 로드 타이밍에 따른 간헐적 초기화 실패 방지)
- 카카오맵 SDK 는 JavaScript 키를 사용하며, 이 키는 브라우저에 노출되는 것이 정상이다.
  카카오 **REST API 키**는 클라이언트에 포함하지 않는다.
- 노출되는 키의 보호는 **카카오 개발자 콘솔의 플랫폼 도메인 등록**으로 한다.
  등록 도메인: `http://localhost:5173` (Vite 기본 개발 포트), Vercel 프로덕션 도메인, Vercel 프리뷰 도메인.

### 8.2 환경 변수

**`.env` (로컬, git 커밋 금지)**

```
VITE_KAKAO_MAP_KEY=<카카오 JavaScript 키>
VITE_SUPABASE_URL=<Supabase 프로젝트 URL>
VITE_SUPABASE_ANON_KEY=<Supabase anon 키>
```

- Vite 는 `VITE_` 접두사가 붙은 변수만 클라이언트에 노출한다. 접두사가 없으면 `undefined` 가 된다.
- 코드에서는 `import.meta.env.VITE_*` 로 접근한다. (`process.env` 아님)

**`.env.example` (git 커밋, 값은 비움)** — 위와 같은 키 목록만 포함

**Vercel 환경 변수** — 동일한 3개 키를 Production / Preview / Development 환경에 각각 등록

규칙:

| 항목 | 내용 |
|---|---|
| 접두사 | 브라우저에서 쓰는 값은 `VITE_` 접두사 필수 |
| `.gitignore` | **`.env`, `.env.local`, `.env.*.local` 포함** |
| 커밋 순서 | `.gitignore` 를 먼저 만든 뒤 `git init` / 첫 `git add` 를 한다 |
| 금지 | `service_role` 키를 클라이언트에 노출하지 않는다 (`VITE_` 접두사로도 금지) |
| 금지 | 카카오 REST API 키(서버용)를 클라이언트에 넣지 않는다 |
| 누락 처리 | 앱 시작 시 3개 변수를 검증하고, 없으면 **어떤 변수가 없는지** 명시해 실패한다 |

> **키 유출 시:** `.env` 가 한 번 커밋되면 파일을 지워도 git 히스토리에 남는다.
> 이 경우 카카오·Supabase 콘솔에서 키를 **재발급**해야 한다. 그래서 `.gitignore` 가 먼저다.

### 8.3 Supabase 작업 방식

테이블 생성, 스키마 변경, RLS 정책 적용은 **Supabase MCP 도구를 통해 수행한다.**

- 대시보드에서 SQL 을 수동으로 붙여넣는 방식으로 진행하지 않는다.
- 적용한 SQL 은 저장소에 남긴다. (`supabase/migrations/`) 스키마를 리뷰·재현할 수 있어야 한다.
- 정책 생성 후 테이블에 **RLS 가 실제로 활성화되었는지** 확인한다.
  RLS 가 꺼진 테이블의 정책은 아무 보호도 제공하지 않으며, 테이블은 전체 공개 상태가 된다.

---

## 9. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 성능 | **첫 마커가 지도에 표시되기까지 3초 이내** (순차 변환이므로 이 지표를 우선한다) |
| 성능 | 카페 250건 기준 좌표 변환 **전체** 완료까지 3분 이내 (순차 처리, 1건당 ~500ms 가정) |
| 성능 | 변환 중에도 지도 조작(이동·확대·마커 클릭)이 막히지 않는다 |
| 성능 | 마커 클릭 후 기존 기록 표시까지 500ms 이내 |
| 성능 | 방문 목록 첫 화면 로드 2초 이내 |
| 성능 | 카테고리·방문 필터 적용은 서버 호출 없이 즉시 반영 (200ms 이내) |
| 반응형 | 모바일(360px) ~ 데스크톱 대응, 모바일 우선 |
| 접근성 | 키보드 조작 가능, 폼 요소 라벨 제공, 마커 상태를 색상만으로 구분하지 않음(모양 병행) |
| 보안 | 모든 개인 기록 접근은 RLS 로 통제 |
| 보안 | 비밀 키는 저장소·클라이언트 번들에 포함하지 않음 |
| 브라우저 | 최신 Chrome / Safari / Edge (모바일 Safari 포함) |
| 언어 | 한국어 단일 |
| 오류 처리 | 네트워크·API 실패 시 사용자가 이해할 수 있는 문구와 재시도 수단 제공 |

---

## 10. 완료 조건 (Acceptance Criteria)

### AC-1. 엑셀 업로드

- [ ] 이름/주소/카테고리 열이 있는 `.xlsx` 를 업로드하면 카페 목록이 인식된다.
- [ ] 필수 열이 없는 파일은 거부되고 없는 열 이름이 안내된다.
- [ ] 이름 또는 주소가 빈 행은 제외되고 제외 건수가 표시된다.
- [ ] 파일 내 `(이름, 주소)` 중복은 1건으로 합쳐진다.
- [ ] 500행 초과 파일은 초과 사실이 안내된다.

### AC-2. 좌표 변환 및 지도

- [ ] 유효한 주소가 마커로 지도에 표시된다.
- [ ] 마커 전체가 보이도록 초기 지도 범위가 조정된다.
- [ ] 변환 중 진행률이 표시된다.
- [ ] 변환 실패 주소가 목록으로 화면에 표시되고, 이름·주소·행 번호·사유를 포함한다.
- [ ] 실패 목록에서 주소를 수정해 재시도할 수 있다.
- [ ] 실패가 있어도 성공한 마커는 정상 표시된다.
- [ ] 변환이 한 건씩 순차로 진행되며, 동시에 여러 요청을 보내지 않는다.
- [ ] 전체 완료를 기다리지 않고 변환되는 대로 마커가 하나씩 나타난다.
- [ ] 변환을 중단할 수 있고, 중단 시점까지의 마커는 유지된다.
- [ ] 필터를 바꾸거나 목록을 다시 업로드해도 **이전 마커가 남아 겹치지 않는다.**
- [ ] 같은 주소가 여러 행에 있어도 좌표 변환 요청은 1회만 발생한다.

### AC-3. 방문 체크 + 소감

- [ ] 마커 클릭 시 방문 체크와 소감 입력 UI가 나타난다.
- [ ] 소감은 200자를 넘길 수 없고 남은 글자 수가 보인다.
- [ ] 저장하면 마커 표시가 즉시 방문 상태로 바뀐다.
- [ ] 비로그인 상태에서는 카페 정보는 보이고 입력만 잠기며 로그인 안내가 보인다.
- [ ] 비로그인 상태에서 로그인한 뒤, 업로드한 목록과 지도 상태가 유지된다.
- [ ] 방문 체크 없이 소감만 저장할 수 있고, 마커를 다시 클릭하면 그 소감이 보인다.
- [ ] 기록을 삭제할 수 있다.

### AC-4. 인증 및 기록 복원

- [ ] 이메일/비밀번호로 회원가입·로그인·로그아웃이 된다.
- [ ] 새로고침 후에도 로그인 상태가 유지된다.
- [ ] 로그인 후 엑셀을 업로드하면, 기존 기록이 있는 카페의 마커가 방문 상태로 표시된다.
- [ ] 이전에 소감을 남긴 카페를 다시 클릭하면 그 소감이 입력창에 그대로 보인다.
- [ ] **같은 카페에 소감을 두 번 저장해도 기록은 1개이고, 내용은 마지막 것으로 갱신된다.**
- [ ] 다른 계정으로 로그인하면 그 계정의 기록만 보인다.

### AC-5. 방문 목록

- [ ] 엑셀 업로드 없이 방문 목록 화면에 진입해 기록을 볼 수 있다.
- [ ] 방문 체크한 카페만 표시된다.
- [ ] 본인 기록만 표시된다.
- [ ] 최근 수정 시각 내림차순으로 정렬된다.
- [ ] 정렬 버튼으로 최신순 ↔ 이름순을 전환할 수 있고, 현재 정렬 기준이 화면에 보인다.
- [ ] 검색으로 항목을 찾을 수 있다.
- [ ] 목록에서 소감 수정·삭제가 된다.
- [ ] 기록이 없으면 빈 상태 안내가 보인다.
- [ ] 방문 체크 없이 소감만 남긴 카페는 이 목록에 나오지 않는다.

### AC-6. 카테고리 필터

- [ ] 업로드한 엑셀의 카테고리 값으로 필터 목록이 자동 생성된다.
- [ ] 카테고리를 선택하면 해당 마커만 지도에 표시된다.
- [ ] 여러 카테고리를 동시에 선택할 수 있다.
- [ ] 아무것도 선택하지 않으면 전체가 표시된다.
- [ ] 각 카테고리의 건수가 표시된다.
- [ ] 카테고리가 빈 카페는 `미분류`로 묶여 필터링된다.
- [ ] 방문 여부 필터와 카테고리 필터가 함께 적용된다.
- [ ] 필터를 바꿔도 보고 있던 지도 위치가 유지된다.
- [ ] 결과가 0건이면 안내 문구가 보인다.

### AC-7. 보안 / 환경 변수

- [ ] `cafe_notes` 에 RLS 가 활성화되어 있다. (정책 존재만이 아니라 **활성화 여부**를 확인)
- [ ] 다른 사용자의 행을 조회·수정·삭제하려는 요청이 거부된다. (계정 2개로 교차 검증)
- [ ] 클라이언트 번들에 `service_role` 키가 포함되지 않는다.
- [ ] 저장소에 `.env` 가 커밋되어 있지 않고, git 히스토리에도 없다.
- [ ] `.gitignore` 에 `.env` 가 포함되어 있고 `.env.example` 이 존재한다.
- [ ] 환경 변수 3개가 모두 `VITE_` 접두사이며 `import.meta.env` 로 읽힌다.
- [ ] 환경 변수 하나를 비우고 실행하면, 어떤 변수가 없는지 명시된 오류가 표시된다.
- [ ] Vercel 환경 변수 3개가 등록되어 배포본에서 지도와 저장이 동작한다.
- [ ] 카카오 콘솔에 개발(`localhost:5173`)·프로덕션·프리뷰 도메인이 등록되어 있다.

---

## 11. 위험 및 대응

| # | 위험 | 영향 | 대응 |
|---|---|---|---|
| R-1 | 엑셀 주소가 부정확해 좌표 변환 실패율이 높다 | 마커 누락 | 실패 목록 노출 + 인라인 주소 수정 재시도 (F-2.8) |
| R-2 | 엑셀에서 카페명/주소를 수정하면 기존 기록과 연결되지 않는다 | 소감 유실처럼 보임 | 정규화로 사소한 차이 흡수(§3.2). 문서·UI 에 "이름+주소가 식별자"임을 안내. 기록은 삭제되지 않고 방문 목록에서 계속 조회 가능 |
| R-3 | 카카오 로컬 API 호출 한도 초과 | 변환 중단 | 순차 처리(F-2.13), 중복 주소 병합, 세션 캐시, 백오프 재시도 |
| R-4 | 대량 행(500건) 처리 시 브라우저 지연 | UX 저하 | 500행 상한, 진행률 표시, 마커 클러스터링 |
| R-10 | 순차 변환으로 500건 처리 시 대기 시간이 길다 | 이탈 | 마커 즉시 표시(F-2.14), 중단 가능(F-2.15), 진행률 표시. 전체 완료 대신 첫 마커 노출 시간을 성능 지표로 삼음(§9) |
| R-11 | SDK 로드 시 `libraries=services` 누락 | 변환 전량 실패 | 초기화 시 `kakao.maps.services` 존재를 검증하고 명시적으로 실패 (§8.1) |
| R-12 | 마커 재렌더 시 이전 마커를 지우지 않아 중첩 | 클릭 오동작·성능 저하 | 재렌더 전 전체 제거를 규칙화 (F-2.16), AC-2 로 검증 |
| R-13 | 정규화 로직이 저장 경로와 조회 경로에서 달라짐 | 저장은 되나 재조회 실패 → 소감이 사라진 것처럼 보임 | 정규화 함수를 단일 모듈로 공유 (§3.2) |
| R-14 | RLS 차단 시 에러가 아니라 빈 결과가 반환됨 | "데이터 없음"과 "권한 없음"을 구분 못 해 디버깅 지연 | 계정 2개로 교차 검증 (AC-7). 빈 결과를 성공으로 단정하지 않음 |
| R-5 | 노출된 카카오 JavaScript 키 오용 | 한도 소모 | 카카오 콘솔 도메인 등록으로 사용처 제한 |
| R-6 | 새로고침 시 업로드 목록 소실 | 재업로드 필요 | 의도된 동작. 방문 기록은 서버에 남으므로 목록 화면으로 접근 가능. UI 에 명시 |
| R-7 | RLS 정책 누락으로 타인 데이터 노출 | 심각 | RLS 활성화를 배포 전 체크리스트에 포함, AC-7 로 검증 |
| R-8 | 비로그인 상태에서 소감을 쓰려다 로그인하면 업로드 목록이 사라진다 | 재업로드 필요 | 로그인을 모달로 처리하거나 복귀 시 목록 유지 (§6.0) |
| R-9 | 미방문 소감이 방문 목록에 없어 사라진 것으로 오해 | 혼란 | 저장 직후 안내 문구 표시 (§5.3) |

---

## 12. 미정 항목 (코드 작성 전 결정 필요)

### 12-A. 엑셀 목록의 서버 저장 여부 — **미정**

현재 문서는 **"저장하지 않음(세션 한정)"** 을 전제로 작성되어 있다. 결정 전까지 이 전제를 유지한다.

| | 저장하지 않음 (현재 전제) | 서버에 저장 |
|---|---|---|
| 사용 흐름 | 접속할 때마다 엑셀 업로드 | 최초 1회 업로드 후 계속 사용 |
| 좌표 변환 | 세션마다 다시 수행 | 1회만 수행, 이후 재사용 |
| 추가 테이블 | 없음 | `cafe_lists`, `cafe_list_items` (+ 각각 RLS) |
| 엑셀 수정 반영 | 새로 업로드하면 즉시 반영 | 재업로드 시 병합/교체 정책 필요 |
| 구현 부담 | 낮음 | 중간 (목록 관리 UI, 동기화 정책) |

**결정이 필요한 시점:** DB 스키마 확정 전. 이 결정에 따라 §7 데이터 모델이 달라진다.

**미정 상태로 진행할 때의 영향:** "저장하지 않음"으로 먼저 만들어도 나중에 저장 기능을
얹을 수 있다. `cafe_notes` 는 `(이름, 주소)` 로 독립적으로 식별되므로 기존 기록은 그대로 유지된다.
따라서 **이 항목이 미정이어도 개발 착수는 가능하다.**

### 12-B. 회원가입 시 이메일 인증 — **미정**

"이메일 + 비밀번호" 방식은 확정되었으나, 확인 메일을 요구할지는 정하지 않았다.
현재 문서는 **인증 메일 없이 즉시 가입 완료**를 전제로 작성되어 있다. (Supabase Auth 설정 한 줄 차이)

- 요구하지 않음: 첫 사용이 빠르다. 오타 이메일로 가입 시 비밀번호 재설정이 불가능해진다.
- 요구함: 계정 신뢰도가 높다. 메일 확인 단계가 하나 늘어난다.

---

## 13. 향후 확장 (참고)

- 카카오 로그인
- 방문 기록 엑셀 내보내기
- 사진 첨부, 별점
- 사용자가 방문 날짜 직접 입력
- 엑셀 목록 서버 저장 및 여러 목록 관리 (§12-A 결정에 따라 MVP 편입 가능)
- 방문 목록 화면의 카테고리 필터
- 목록 공유(읽기 전용 링크)

---

## 14. 결정 로그

| # | 항목 | 결정 | 반영 위치 |
|---|---|---|---|
| D-1 | 인증 방식 | 이메일 + 비밀번호 | §5.4, §8 |
| D-2 | 인증 경계 | **지도·업로드는 공개, 기록 저장은 로그인 필요** | §6.0 |
| D-3 | 상한값 | 소감 200자 / 500행 / 5MB 그대로 확정 | §5.1, §5.3, §7.2 |
| D-4 | 카테고리 | **표시 전용 → 지도 필터로 승격 (MVP 포함)** | §5.6, AC-6 |
| D-5 | 방문 날짜 | 사용자 입력 없음. 저장 시각(`updated_at`)만 자동 기록 | §7.2, §2.2 |
| D-6 | 미방문 소감 | **허용.** 단 방문 목록에는 노출하지 않고 저장 시 안내 문구 표시 | §5.3, §5.5 |
| D-7 | 카카오 로그인 | MVP 제외 | §2.2, §13 |
| D-8 | 엑셀 내보내기 | MVP 제외 | §2.2, §13 |
| D-9 | 엑셀 서버 저장 | **미정** — "저장하지 않음" 전제로 진행 | §12-A |
| D-10 | 이메일 인증 메일 | **미정** — "요구하지 않음" 전제로 진행 | §12-B |
| D-11 | 빌드 도구 | **Next.js → Vite + React 로 변경.** 환경 변수 접두사 `NEXT_PUBLIC_` → `VITE_` | §8, §8.2 |
| D-12 | 좌표 변환 방식 | **동시 4~5개 → 순차 1건씩으로 변경.** 마커는 도착하는 대로 즉시 표시, 중단 가능 | §5.2, F-2.13~15 |
| D-13 | 성능 지표 | 전체 완료 시간(250건 30초) → **첫 마커 노출 시간(3초)** 을 주 지표로. 전체 완료는 3분 이내 | §9 |
| D-14 | Supabase 작업 | 테이블·RLS 정책은 **Supabase MCP 도구**로 적용, SQL 은 저장소에 보관 | §8.3 |
| D-15 | 개발 순서 | 컴포넌트를 가짜 데이터로 완성 후, 마지막에 지도·엑셀·Supabase 연결로 교체 | CLAUDE.md 규칙 5 |

> 구현 규칙의 상세(SDK 로딩 방식, 마커 정리, upsert 충돌 지정, 흔한 실패 모드 등)는
> `CLAUDE.md` 의 Implementation rules 를 따른다. 본 문서와 충돌하면 `CLAUDE.md` 가 우선한다.
