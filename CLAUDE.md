# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

엑셀로 정리한 카페 목록을 카카오맵에 마커로 표시하고, 로그인한 사용자의 방문 소감을
Supabase Database에 저장하는 지도 서비스. 상세 스펙은 `PRD.md` 참고.

## 현재 상태

코드 없음. `PRD.md`(한국어, v0.3)와 이 문서만 있다. `package.json`·git·테스트 도구 모두 없어서
실행할 명령이 아직 없다. 스캐폴딩은 요청받은 뒤에 한다. 코드가 생기면 여기에 빌드/테스트 명령을 적는다.

## 기술 스택

React + Vite, Tailwind + shadcn/ui, xlsx, 카카오맵 JS SDK, Supabase(Auth/Database, MCP로 관리)

## 반드시 지킬 규칙

- 카카오맵 키는 `.env`의 `VITE_KAKAO_MAP_KEY`로 관리한다.
- 카카오맵 SDK는 `libraries=services`를 포함해서 불러온다 (주소→좌표 변환에 필요).
- Supabase URL과 anon key는 `.env`의 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`로 관리한다.
- 소감 테이블은 RLS를 켜고 "본인 데이터만" 정책을 적용한다.
- `.env`는 `.gitignore`에 포함한다.
- 화면은 가짜 데이터로 먼저 만들고, 마지막에 실제 연동으로 교체한다.
- 마커를 다시 그릴 때는 이전 마커를 모두 지운 뒤 새로 그린다.
- 주소→좌표 변환은 하나씩 순서대로, 실패는 화면에 안내한다.
- 소감 저장은 upsert로, 같은 장소(이름+주소) 기록은 1개만 유지한다.
- Supabase 관련 작업(테이블, RLS)은 Supabase MCP 도구로 수행한다.

## 구조 핵심

**카페 마스터 테이블이 없다.** 엑셀이 원본이고, 서버는 사용자가 소감을 남긴 장소만 안다.
장소 식별자는 `이름 + 주소`이며, `cafe_notes`에 `UNIQUE (user_id, name_key, address_key)`로 강제한다.
`*_key`는 정규화값(trim·공백축약·NFC·대소문자 무시), 원본 문자열은 화면 표시용으로 따로 보관.

**데이터 수명이 두 갈래다.** 업로드한 목록·좌표 캐시·필터는 브라우저 메모리(새로고침 시 소실이 의도),
세션과 소감만 Supabase에 영구 저장. 그래서 소감에 `lat`/`lng`를 함께 저장한다 —
방문 목록 화면이 엑셀 없이 지도로 이동할 수 있어야 하기 때문.

**백엔드 API가 없다.** 브라우저가 Supabase에 직접 붙으므로 RLS가 유일한 인증 계층이다.
카카오맵도 프록시 없이 프론트에서 직접 호출한다. JS 키는 공개가 정상이고,
카카오 콘솔 도메인 등록(`localhost:5173`, Vercel 프로덕션/프리뷰)으로 보호한다. REST 키는 쓰지 않는다.

**인증 경계:** 업로드·지도·필터는 비로그인 가능, 소감 읽기/쓰기만 로그인 필요.
로그인시켜도 업로드한 목록이 날아가지 않게 한다 (모달 권장. `/login` 리다이렉트로 깨지기 쉬움).

## 흔한 실수

- **SDK**: `autoload=false` + `kakao.maps.load()` 안에서 초기화. 스크립트는 한 번만 주입.
  `window.kakao.maps.services`가 없으면 `libraries=services`가 빠진 것 — 조용히 넘기지 말고 실패시킨다.
- **upsert**: 충돌 대상을 명시해야 한다 (`onConflict: 'user_id,name_key,address_key'`).
  안 넣으면 일반 insert로 동작해서 제약 위반 에러. `user_id`는 `auth.uid()`에서 가져온다.
- **정규화**: 저장 경로와 조회 경로가 **같은 함수**를 써야 한다. 다르면 저장은 되는데 다시 못 찾아서
  소감이 사라진 것처럼 보인다.
- **RLS**: 정책에 막히면 에러가 아니라 **빈 배열**이 온다. "데이터 없음"과 "권한 없음"이 구분되지 않으니
  계정 2개로 교차 검증한다. 정책만 만들고 RLS를 안 켜면 테이블은 전체 공개다.
- **마커 제거**: 배열 참조만 버리면 지도에서 안 지워진다. 각 마커에 `setMap(null)` 후 배열을 비운다.
  필터 변경도 재렌더다. 단, 필터 변경 시 지도 범위는 다시 맞추지 않는다(보던 위치 유지).
- **지도 컨테이너**: 높이를 명시하지 않으면 0px로 잡혀서 에러 없이 빈 화면이 된다.
  숨겨졌다가 보이거나 크기가 바뀌면 `relayout()` 호출.
- **세션**: `getSession()`이 비동기라서 로그인 상태인데 잠깐 비로그인으로 보인다.
  이 사이에 "기록 없음"을 띄우거나 `/login`으로 보내면 버그. `loading` 상태를 따로 둔다.
- **엑셀**: 셀을 문자열로 읽는다. 숫자·날짜 자동 변환이 주소를 망가뜨려 키 매칭이 깨진다. CSV는 BOM 제거.
- **Supabase 클라이언트**: 공용 모듈에서 한 번만 생성. 컴포넌트 안에서 만들면 렌더마다 재생성된다.
- **StrictMode**: 개발 모드에서 effect가 2번 실행돼 SDK 중복 로드·좌표 변환 2회·마커 2배가 된다.
  StrictMode를 끄지 말고 가드로 막는다.

## 제한값

엑셀 5MB / 500행, 소감 200자(UI 카운터 + DB `CHECK` 양쪽). 좌표 변환은 순차라 느리다 —
첫 마커 3초 내 노출을 우선하고, 도착하는 대로 그리며 중단 가능하게 한다. (PRD §9)

## 미정 (한쪽으로 임의 결정하지 말 것)

- **엑셀 목록 서버 저장** (PRD §12-A): "저장 안 함" 전제. 나중에 추가해도 기존 소감은 유지되므로 착수 가능.
- **가입 시 확인 메일** (PRD §12-B): "요구 안 함" 전제. Supabase Auth 토글 하나.

MVP 제외(누락이 아니라 결정): 카카오 로그인, 엑셀 내보내기, 방문 날짜 직접 입력, 사진, 별점, 공유.

## PRD.md 수정 시

요구사항 ID(`F-*`, `AC-*`, `R-*`, `D-*`)가 문서 전체에 상호 참조되어 있다. 스코프가 바뀌면
결정 로그(§14)에 기록하고 버전을 올린다. 문서는 한국어. 이 파일과 PRD가 충돌하면 이 파일이 우선.
